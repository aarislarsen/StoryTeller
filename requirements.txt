# Pinned to the exact versions verified working, for reproducible / airgapped
# installs. Install with:  pip install -r requirements.txt
# (Transitive deps of these are resolved/downloaded automatically by pip.)

# Web framework + WSGI toolkit
flask==3.1.3
werkzeug==3.1.8

# Real-time (Socket.IO) stack. The Socket.IO protocol version here must stay
# compatible with the bundled client in static/js/vendor/socket.io.min.js (v4.x).
flask-socketio==5.6.1
python-socketio==5.16.2
python-engineio==4.13.2
simple-websocket==1.1.0

# Async server backend used by flask-socketio.
# NOTE: eventlet is in maintenance/deprecation mode; consider migrating to
# async_mode='threading' (uses simple-websocket, already pinned above).
eventlet==0.41.0
